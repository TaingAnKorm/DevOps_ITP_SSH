adc_file: "/home/kta/ansible-iac/gcp-key.json"
google_project_id: "feisty-script-469614-h4"
ssh_privkey_path: "/home/kta/.ssh/ansible_ed25519"
ssh_pubkey_path: "/home/kta/.ssh/ansible_ed25519.pub"
agent_user: "kta"
machines_specs: 
  large:
    machine_type: "e2-standard-2"
    zone: "asia-southeast1-a"
    disk_size: 20
    disk_type: "pd-standard"
  medium:
    machine_type: "e2-medium"
    zone: "asia-southeast1-a"
    disk_size: 20
    disk_type: "pd-standard"
ubuntu_specs:
  v22:
    image: "projects/ubuntu-os-cloud/global/images/family/ubuntu-2204-lts"
machines_info:
  - name: "master01"
    machine_type: "{{ machines_specs.large.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{ machines_specs.large.zone }}"
    disk_size: "{{ machines_specs.large.disk_size }}"
    disk_type: "{{ machines_specs.large.disk_type }}"
  - name: "master02"
    machine_type: "{{ machines_specs.large.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{ machines_specs.large.zone }}"
    disk_size: "{{ machines_specs.large.disk_size }}"
    disk_type: "{{ machines_specs.large.disk_type }}"
  - name: "master03"
    machine_type: "{{ machines_specs.large.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{ machines_specs.large.zone }}"
    disk_size: "{{ machines_specs.large.disk_size }}"
    disk_type: "{{ machines_specs.large.disk_type }}"
  - name: "worker01"
    machine_type: "{{ machines_specs.medium.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{ machines_specs.medium.zone }}"
    disk_size: "{{ machines_specs.medium.disk_size }}"
    disk_type: "{{ machines_specs.medium.disk_type }}"
  - name: "worker02"
    machine_type: "{{ machines_specs.medium.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{ machines_specs.medium.zone }}"
    disk_size: "{{ machines_specs.medium.disk_size }}"
    disk_type: "{{ machines_specs.medium.disk_type }}"
